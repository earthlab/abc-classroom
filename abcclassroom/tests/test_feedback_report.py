# This will require having a dummy notebook fixture which represents a
# student assignment

# Test that a notebook with ### BEGIN HIDDEN TESTS gets scrubbed properly

# Test what happens when they misspell the assignment name - it should fail
# gracefully

# Test what happens when the autograded directory for a student doesn't exist

# Test what happens when the feedback directory for a student doesn't exist

# Test what happens when the autograded and feedback directories (the root dir
# don't exist

# Test what happens when there are no notebooks to open
